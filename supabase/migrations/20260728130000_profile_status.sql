-- ============================================================================
-- profiles.status — 마이페이지 한 줄 상태 메시지 (SNS처럼 직접 작성)
--
-- profiles 는 이미 RLS 정책이 있다:
--   - select: 로그인 사용자 전체 허용(작성자 이름 표시용)
--   - update: id = auth.uid() (본인만)
-- 따라서 본인만 자기 상태 메시지를 수정할 수 있다. 별도 정책 불필요.
-- ============================================================================
alter table public.profiles
    add column if not exists status text;
